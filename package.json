{
  "name": "claude-peek",
  "version": "0.1.7",
  "description": "A clean, compact statusline for Claude Code — context, usage, git, todos at a glance.",
  "type": "module",
  "main": "statusline.mjs",
  "author": "classting",
  "license": "MIT",
  "keywords": ["claude", "statusline", "hud"],
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
